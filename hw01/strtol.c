#include <stdio.h>
#include <stdint.h>

long int mystrtol(const char *nptr, char **endptr, int base)
{
    char c = *nptr;//   12345
    while( isspace(c) )
    {
        c = *nptr++;
    }

    if( c == - )
    elif( c == + )

    if( base == ..)

    
}